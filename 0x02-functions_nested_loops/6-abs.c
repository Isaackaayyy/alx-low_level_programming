#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 *
 * @n: first value
 *
 * Return: integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (1);
	}
	else
		return (0);
}

