#include "main.h"

/**
 * print_alphabet - the alphabet, in lowercase
 *
 *
 * Return: integer
 */

int print_alphabet(void)
{
	int i;

	for (i >= 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	return (0);
}
